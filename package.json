{
  "name": "didahsdr-devtools",
  "private": true,
  "description": "Development tooling only (lint, tests). The app itself (app/) has no npm dependencies and no build step.",
  "license": "AGPL-3.0-or-later",
  "scripts": {
    "lint": "eslint app/js tests eslint.config.js",
    "test": "node --test tests/js/"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.11.0",
    "globals": "^17.12.0"
  }
}
